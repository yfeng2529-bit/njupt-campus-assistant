# 验证报告

日期：2026-09-10。此版本为本地应用，每人配置自己的 API，不提供共享服务。

## 已执行的验证

| 检查 | 结果 |
| --- | --- |
| 原有 Windows / Python 3.14 环境 | 10 项回归测试通过 |
| 无 .venv 的独立安装目录 | setup.ps1 自动下载 Python 3.12.10 NuGet 运行环境并安装全部依赖，成功 |
| 新安装的 Python 3.12 环境 | 10 项测试通过，真实 BGE 检索通过 |
| 桌面快捷方式 | 已创建，目标为本项目 run_app.bat |
| 本地启动器 | 后台启动成功，仅绑定 127.0.0.1，健康检查正常 |
| 完全退出与重启 | stop_app.ps1 仅停止匹配本项目的进程，退出和重启验证通过 |
| 知识库 | 1,047 条片段，向量形状 (1047, 512)，数据指纹一致 |
| BGE 输入长度 | 最长 430 tokens，模型上限 512，超限 0 个 |
| 真实考试作弊检索 | 命中考试纪律第十一、十二、十三条等；第十二条跨 PDF 282–283 页 |
| 实际 API 问答（同日基础版本） | 现有默认 API 成功返回回答；本轮未额外重复收费测试 |
| 待发布文件扫描 | 源码、文档、数据共 34 个文件中未发现已有真实密钥 |
| 个人文件忽略规则 | .env、API 配置、聊天、.venv、.runtime、.local、备份均被 Git 排除 |

## 测试范围

测试覆盖聊天保存/恢复/清空和损坏数据保护；配置迁移、稳定 UUID、编辑和删除默认后的回退；知识库缺失与数量不一致；检索去重、追问识别、历史上限及无来源时不调用 API；实际页面新增/编辑、密钥不回填、历史来源保留、清空记录；即使保留旧共享环境变量，网页仍要求配置自己的本地 API。

页面网络操作使用替身，避免修改真实配置和重复消耗 API。真实向量检索使用实际 BGE 模型和最终知识库。首次安装测试使用已有系统用户的下载缓存，但 Python 运行环境及依赖安装目录全新；不代表所有网络环境都能顺利下载。

## 尚未声称完成的验证

未建立完整人工评测集，不能宣称回答准确率。未测试所有 Windows 版本、低内存设备和无网络安装。应用为本地单人使用，未做多用户并发和多窗口写入协调。

## 复查命令

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests -v
.venv\Scripts\python.exe search_test.py
```

